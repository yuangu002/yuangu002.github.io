# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Yuan Gu
author: Yuan Gu
email: guyuan002@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Carpe Diem, Seize the Day
github_username:  yuangu002
linkedin_username: gu-yuan
url: "https://yuangu002.github.io"
baseurl: ""

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Exclude from processing. GitHub Pages runs Jekyll 3, where this list replaces the
# default one, so the defaults are repeated here.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/
  - publish.sh
  - test.sh
  - tag_generator.py

# Keep the windpig report pages out of the sitemap.
defaults:
  - scope:
      path: "windpig"
    values:
      sitemap: false
